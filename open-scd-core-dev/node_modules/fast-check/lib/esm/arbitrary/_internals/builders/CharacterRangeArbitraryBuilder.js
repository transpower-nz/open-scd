import { fullUnicode } from '../../fullUnicode.js';
import '../../../stream/StreamHelpers.js';
import { encodeURIComponent as SencodeURIComponent, safeNumberToString, safeCharCodeAt } from '../../../utils/globals.js';

function percentCharArbMapper(c) {
    const encoded = SencodeURIComponent(c);
    return c !== encoded ? encoded : `%${safeNumberToString(safeCharCodeAt(c, 0), 16)}`;
}
function percentCharArbUnmapper(value) {
    if (typeof value !== 'string') {
        throw new Error('Unsupported');
    }
    const decoded = decodeURIComponent(value);
    return decoded;
}
fullUnicode().map(percentCharArbMapper, percentCharArbUnmapper);
//# sourceMappingURL=CharacterRangeArbitraryBuilder.js.map
